on:
  push:
    branches:
      - main # Triggers the workflow on push events to the main branch
jobs:
  build:
    runs-on: self-hosted  
    steps:
      - name: Clean up temporary files
        run: sudo rm -rf /tmp/* 
